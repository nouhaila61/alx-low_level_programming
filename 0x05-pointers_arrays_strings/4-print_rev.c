#include "main.h"

/**
 * print_rev - prints a given string in reverse
 * @str: the string
 */

void print_rev(char *str)
{
	int len = 0;

	while (s[len])
	{
		len++;
	}

	for (int i = len - 1; i >= 0; i--)
	_putchar(s[i]);

	_putchar('\n');
}

