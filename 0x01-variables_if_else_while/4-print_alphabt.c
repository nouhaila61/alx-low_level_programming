#include <stdio.h>
/**
 * main - prints tha alphabet in lowercase
 *
 * Return: 0
*/

        int main(void)
{

        char x;

        for (x = 'a'; x <= 'z'; x++)
{
	if (letter != 'e' && letter != 'q')
	putchar (x);
} 
        putchar ('\n');
        return (0);
}
