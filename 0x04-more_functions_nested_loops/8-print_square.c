#include "main.h"

/**
  * print_square - prints a square
  * @size: The number of lines to draw
  * Return: empty
  */
void print_square(int size)
{
	int x, y;

	if (size <= 0)

		_putchar('\n');

		for (x = 0; x < n; x++)
		{
		for (y = 0; y < x; y++)

		_putchar(35);

	_putchar('\n');
		}
}

