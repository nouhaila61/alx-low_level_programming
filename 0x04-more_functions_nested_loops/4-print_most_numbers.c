#include "main.h"

/**
 * print_most_numbers -  prints the numbers, from 0 to 9 , except 2 and 4
 */
void print_most_numbers(void)
{
	char x;

	for (x = '0'; x <= '9'; x++)
	{
		if (i == '2' || i == '4')

		continue;

	_putchar(x);
	}

		_putchar('\n');
}

